# KickAir — Session Handoff

Context for continuing work on KickAir, a Fiverr-like freelance marketplace. Read this first, then verify the current git state on both repos before starting anything new.

## Repos & stack
- **Backend:** `kickair-api` — Laravel 12 + PostgreSQL + Sanctum (local Postgres runs via **DBngin**, start it from the app; db `kickair`, user `postgres`)
- **Frontend:** `kickair-frontend` — Next.js 16 (App Router) + Panda CSS + Ark UI + lucide (MUI/Emotion fully removed 2026-09-13 on the `mui-removal` branch; `MIGRATION-PANDA.md` is the styling rulebook)
- The **admin console lives inside the frontend** under the `(admin)` route group (`/admin/...`), built with **Panda CSS** (`src/components/admin/`), like the rest of the site now
- Both repos push directly to `main` (GitHub: `xSothkimleng/kickair-frontend` and `/kickair-api`)
- **Payments are mocked** (no real gateway); seller-only 20% commission (`KICKAIR_COMMISSION_RATE`, 0 in tests via phpunit.xml)
- **Production:** frontend on Vercel, API on Railway (start.sh migrates + seeds-if-empty on deploy); uploads live on **Cloudflare R2**

## Working preferences (important)
- Commits: clean messages, **no `Co-Authored-By: Claude` lines**
- Push directly to `main`, and **only when explicitly asked**
- Verify before declaring done: `npx tsc --noEmit` (frontend) and `./vendor/bin/pest` (backend; 396 tests, all green at handoff)
- **Postgres gotcha:** enums are CHECK constraints — alter them with raw
  `ALTER TABLE ... DROP CONSTRAINT / ADD CONSTRAINT ... CHECK (...)`, never MySQL `MODIFY`
- **Client feedback loop:** notes from testing sessions land as files in `../kickair-feedback/`; the `/feedback` skill (in `../.claude/skills/`) triages them into a reviewable todo page saved under `kickair-feedback/rounds/` (gitignored). Triage first, execution only after review.

## Seeded test logins (all password: `password`)
| Email | Role |
|-------|------|
| john@example.com (id 1) | Freelancer |
| jane@example.com | Freelancer |
| bob@example.com | Freelancer + Client |
| alice@example.com | Client |
| charlie@example.com | Client |
| admin@kickair.com | Admin |

Email sign-in is **case-insensitive** (stored lowercase; model mutator + request trait).

## Status after client feedback round 3 (notes: 8.25.2026.pdf — 25 of 26 actionable done, 9 parked)
Full per-task log with outcomes: `kickair-feedback/rounds/8.25.2026-todo.html` (also published as a Claude artifact).

### Round 3 highlights — where things live
- **Custom orders = single payment** — milestone UI removed from OfferComposer / DirectOfferDialog / ReviewOffer; fee-deduction card in the composer sidebar.
- **Custom orders merged into orders (unified flow)** — a custom order is only the *negotiation* (request → offer → accept). Accepting a single-payment offer sets `custom_orders.flow='order'` and the created Order runs the **regular lifecycle**: deliver/approve/revision/dispute/timeline/review (review lands on the anchored service). Legacy accepted milestone orders keep `flow='milestone'` + the Workspace; milestone endpoints reject order-flow orders. The Custom Orders / Custom Requests **tabs are gone** — negotiation-phase custom orders (request → offer) render as **rows in the unified Orders list** on both sides (`client/OrdersContent`, `freelancer/OrdersContent`, with a "requests" filter chip); the old strip components were removed; old `?tab=` links redirect. Both dashboard pages now follow `?tab=` changes after mount (`useSearchParams`), so notification "View" links that only change the tab work while the dashboard is already open. Order model has three-origin accessors (`freelancer`, `service`, `display_title`) used across controllers/notifications.
- **Freelancer leveling** — Bronze→Diamond thresholds existed; now profile-completion steps award **one-time XP** (12 steps, 135 total, `ProfileXpService` + `profile_xp_awards` column, synced on `/freelancer-dashboard/level`). "Your level" dialog (`LevelDialog`) opens from the profile-strength card + level badge; badge also on the service detail page. Fixed orders double-awarding XP/completed-count (observer is the single source now).
- **Media rules** — publishing a service **requires ≥1 image** (server-enforced on create/publish/last-image-delete; drafts exempt); `feature_image_id` must be an image. `serviceCoverUrl()` (lib/serviceCover.ts) picks card covers (feature image, else first image-type media — never a PDF). Detail page gallery plays **videos**; PDFs listed in a **Documents** card.
- **Auth/UX fixes** — post-verification auto-redirect to Explore Services; PHP upload caps raised to 52M/60M (start.sh + local php.ini) — the "portfolio can't upload" bug; localStorage (form recovery, client/freelancer mode) scoped per account; draft saves never blocked (incomplete pricing/FAQ rows dropped, tier rule skips drafts); dialogs restyled (titles in body, no DialogTitle bars — keep this pattern for new dialogs); admin user table sorts server-side; admin about fields render rich text; mobile filter collapse on Explore Services.

### Open items
- **#22 (last open decision):** "add more detail on custom order card" — context changed twice; the card in question is now the **request row in the unified Orders list** (see the 2026-09-06 status below). Ask Kimleng which details.
- **Parked by client (9):** order-detail file collection removal, "feedback:" in order detail, dispute-flow items (#29, #31–34 — now automatically applicable to custom orders thanks to the unified flow), IAM in admin dashboard.
- Pre-existing lint debt: a few react-compiler errors in `Workspace.tsx` (static-components) and setState-in-effect in dashboard page.tsx files — untouched, not from this work.

## Status after the 2026-09-06 session (Kimleng's post-round-3 requests) — committed & pushed
Frontend `4be37d8`, API `a47c431`. Both trees clean.

### What changed — where things live
- **Unified Orders list on both spaces** — negotiation-phase custom orders are rows in `client/OrdersContent` and `freelancer/OrdersContent` ("requests" filter chip with an "N new" badge; client gets Review offer / Withdraw, freelancer gets Make an offer / Decline). `CustomOrdersContent` and `CustomRequestsInbox` are deleted. The freelancer's pending-request detail (inline `OfferComposer`, `?compose=1` deep link) lives in `dashboard/custom-orders/[id]/page.tsx`; the composer is stacked (form above summary, full width).
- **Order Record is the single order history** — `components/dashboard/OrderRecord.tsx` (events + numbered deliveries/revisions + files) is used by the client, freelancer **and admin dispute** pages. The delivered-work card on order detail, `OrderTimeline` and `DeliverablesReference` are gone. Kimleng's rule: don't add parallel history cards — put history in the record. Its event log is a React Query query (`qk.orders.timeline(id)`), so any `invalidateQueries(qk.orders.all())` — after an action on the page or a realtime notification — refreshes it live (2026-09-06; before that it fetched once on mount and went stale). The client page also passes the custom request/offer pre-events like the freelancer and admin pages do. Job orders now log `order_accepted` when the client approves the proposal (`ProposalController`), so their record no longer starts at the first delivery; older job orders (no backfill, Kimleng's call) get a synthetic "Order Placed" row from `created_at` because the record adds one whenever the log has no start event.
- **Notifications** — `getNotificationRoute` sends custom-order request/decline/withdraw to `/dashboard/custom-orders/{id}`. Both dashboard `page.tsx` files read `?tab=` via `useSearchParams` (Suspense-wrapped) and follow changes after mount, so tab-only links work while the dashboard is already open.
- **Service form** — `EarningsBreakdown` renders at the foot of `PricingSection`, inside the Pricing Options card.
- **Admin dispute page** — Order Record; "Agreed custom offer" card for custom-origin orders (brief, budget, timeline, scope, price, delivery, revisions, note); "Earlier disputes on this order"; Trust & Safety shows an error state instead of a fake-empty queue. `AdminDisputeResource` uses the Order origin accessors — custom-origin orders used to 500 the whole queue. `OrderCancellationService` had the same two-origin bug, fixed. The dispute itself is a React Query query (`qk.disputes.adminDetail(id)`); `admin_dispute_*` alerts invalidate disputes + orders in `realtimeInvalidation.ts`, so the page and its Order Record update without a reload. Gap: parties' `evidence_submitted` notifications go only to the counterparty, not admins, so evidence still needs a focus refetch on the admin page. Disputes are separate records, so after a "continue" the next dispute has a new id/URL; `AdminDisputeResource` exposes `newer_dispute` and the page shows a live banner linking to it (no auto-jump — Kimleng preferred the banner).

### Dispute model (agreed with Kimleng 2026-09-06)
- Disputes **stack** per order and are **numbered** (`disputes.sequence` → "Dispute #1, #2, …"); only **one open at a time** (so #2 can never be resolved before #1). `Order::dispute()` = latest (`latestOfMany`), `Order::disputes()` = history, `Order::disputeSummaryLabel()` = "Disputes #1, #2 and #3".
- Four outcomes. `full_freelancer` / `full_client` / `partial` end the order. **`continue`** ("Continue with admin feedback") is a *real* resolution: no money moves, the order goes back to **`active`** (freelancer sees Submit Delivery, client sees the waiting view), and either party may dispute again.
- **Order record rows:** `dispute_opened` ("Dispute #n was opened by the … Reason: …"); one `dispute_feedback` row per continue, rendered as **"Dispute #n"** with "Admin feedback: …" (the frontend derives n by counting opened rows); and exactly **one** `dispute_resolved` summary when the matter actually ends — written by an admin money outcome ("Disputes #1 and #2 resolved — the client was refunded in full. Admin feedback: …") or by client approval in `OrderController::approve` ("… — the client accepted the work."). Earlier rows are never rewritten.
- Admin UI: continue-outcome disputes show a blue **Continued** pill (list + detail) and sit under the Resolved filter.
- Migration `2026_09_06_100000_allow_stacked_disputes` drops the one-per-order unique, adds `sequence`, extends the `outcome` CHECK. **Rollback fails on any DB that already holds a `continue` outcome** (the old constraint rejects it) — expected for an enum extension.

### Open items (unchanged)
- **#22** — which extra fields on the request row. Client's 9 parked items. Lint debt.

## Status after the 2026-09-07 session (Telegram Gateway hardening) — committed & pushed
API commit only; frontend untouched apart from this note.

### Decision: keep Telegram Gateway for phone OTP
- A free **bot + `request_contact`** flow was designed (user opens a bot, shares contact, Telegram vouches for the number via `contact.user_id === from.id`). Kimleng chose to stay on Gateway: codes come from the official verified **Telegram** account, the user never leaves the app, zero webhook infra. Revisit the bot plan only if Gateway pricing changes.
- Gateway is $0.01 per **delivered** code with a **$100 minimum top-up via Fragment/TON**, non-refundable. **Codes to your own number are free** — that is how dev/testing works today. Kimleng will fund at launch and **rotate the token afterwards** (only `.env` / prod env hold it; nothing else references the value).
- Dashboard: https://gateway.telegram.org — not my.telegram.org, not BotFather. The `gatewayapi.telegram.org` host in `TelegramGatewayService` is the machine endpoint only.
- After funding it just works for any Telegram-registered E.164 number — no approval, no allowlist. Non-Telegram numbers fail at send with no charge (already surfaced as a 422).

### What changed — where things live
- `routes/api.php` — `send-otp`, `register/phone`, `phone/update` use the named **`phone-code`** limiter (`AppServiceProvider::boot`): **5/min per phone number + 30/min per IP**. Bare `throttle:5,1` keys on IP only and shares one bucket across every route that uses it, so two resends plus two typos would have locked the user out — and everyone behind the same carrier NAT with them.
- `TelegramGatewayService::send()` passes `ttl` (900s, fed from `PhoneVerificationManager::TTL_SECONDS` through the new `PhoneVerificationChannel::send(string $phone, int $ttlSeconds)` signature). Telegram refunds codes it cannot deliver inside the ttl and deletes them from the chat on expiry.
- `.env.example` / `.env` — `TELEGRAM_GATEWAY_TOKEN` documented (was missing entirely; local `.env` holds Kimleng's dev token, gitignored).
- Tests: ttl assertion, send-otp throttle, register/update throttle, per-phone isolation. Suite: 400 passing.

### Deferred — for Kimleng's later "optimize / refactor / security" pass (after the whole project is signed off)
- Throttled users see Laravel's default "Too Many Attempts." in the sign-up / settings error box — friendlier copy wanted.
- `forgot-password`, `reset-password`, `email/resend-link` still share one bare per-IP `throttle:5,1` bucket — same gotcha, pre-existing; give them a named limiter.
- `login/email` and `login/phone` have **no throttle at all** — credential stuffing is open. Not touched this session; top of the security list.
- Optional: Gateway `checkSendAbility` pre-check to bounce non-Telegram numbers to email before the OTP step (it moves the fee, doesn't add one). Sign-up copy still says "we'll text you a code".

## Status after the 2026-09-07 session, part 2 (admin console replaced) — committed & pushed
Kimleng approved the `/temp-dash` mock-up (white canvas, clean sidebar) and asked for it to become the real admin panel. Shipped in one frontend commit and one API commit the same evening (the two must deploy together — the console depends on the new endpoint shapes). `@mui/x-charts` was removed in the same commit; it had no users left. MUI itself still styles the whole user-facing site (141 files), so it stays until the site is migrated to Panda.

### What changed — where things live
- **Frontend** — the old MUI admin (`components/admin/{dashboard,trust,users,marketplace,payments,config,analytics,monitoring}`, `AdminSidebar`, `adminKit`, `AdminNotificationBell`) is **deleted**. The console is now `src/components/admin/` (Panda CSS, `--td-*` tokens in `ui.tsx`): `Shell.tsx` (sidebar with live queue counts, people search, bell, admin-only guard, realtime bridge), one `*Page.tsx` per route, `queries.ts` (React Query hooks under the `qk.admin.*` prefix + `useAdminAction`), `notify.ts` (admin notification → route), `toast.tsx`, `format.ts`, `labels.ts`. Routes: `/admin` overview, `/admin/verifications`, `/admin/disputes[/:id]`, `/admin/listings` (`?kind=job`), `/admin/finance`, `/admin/people[/:id]` (`?q=` from the topbar search), `/admin/catalog`, `/admin/inbox`, `/admin/login`. `next.config.ts` redirects the old paths (`/admin/trust`, `/users`, `/marketplace`, `/payments`, `/notifications`, `/config`, `/analytics`, `/monitoring`, `/temp-dash/*`).
- **Realtime** — `GlobalNotificationToast` is still mounted in the shell; `registerAdminRefresh` / `registerBellRefresh` now invalidate `qk.admin.all()` + notification queries, so every queue and badge refreshes on an admin alert. The dispute page keeps `qk.disputes.adminDetail(id)` and embeds the shared MUI `OrderRecord` (single-history rule).
- **Dropped on purpose** (stubs with no backend): Analytics, Monitoring/audit log, revenue charts, financial reports, hiring & reviews tabs, and the Config page's General/Pricing/CMS/Localization tabs. Categories & Skills survive as Catalog. The user's "Activity" tab from the mock-up (per-user transactions, listings, disputes) has no per-user endpoints, so the People detail page shows the activity counters the detail endpoint already returns instead.
- **API** — `GET /admin/dashboard/stats` adds `disputes.open_count`, `listings.pending_services/pending_jobs` and `oldest_at` per queue (sidebar badges + overview cards). `GET /admin/users` accepts `status=active|suspended|banned` and `AdminUserResource` exposes `suspended_at`/`banned_at`. `AdminDisputeResource` exposes `opened_by`. Category list carries `services_count`, skills carry `freelancer_profiles_count` (delete is blocked in the UI while in use — category delete **cascades to services** at the DB level). `GET /admin/services`, `/admin/job-posts`, `/admin/kyc` now return `{ data, meta }` like the other admin lists (`api.ts` signatures updated; `KycTest` assertions moved to `data.data`). Admin notification `link`s point at the new routes; the frontend ignores stored links anyway (`notify.ts` routes by type, so older notifications still land in the right queue).
- Verified: `tsc` clean, eslint clean on the console, pest 400 green, every route rendered against the local API via headless Chrome.

### Known gaps / follow-ups
- Dispute chat: `Message` files render as a link; no upload from the admin side (same as before).
- Verification drawer shows the document images + account details; there is no legal-name / DOB / document-number capture in KYC, so the mock-up's "name match" check has no data behind it.
- `ago()` in the console floors days (a 45-hour-old item is "1d ago" under the "Yesterday" header); the queue "waiting" labels round.

## Next: remove MUI from the user-facing site (Kimleng's ask, 2026-09-07 — starts in a new session)
Kimleng wants MUI gone. The admin console is already Panda; the **site** is the whole job. `@mui/x-charts` is already removed (no users).

### Inventory (2026-09-07, after the console shipped)
- **141 files import `@mui/`** (~30,000 lines, ~4,000 `sx=` props). Biggest single files: `(main)/settings/page.tsx` (1,071 lines), `service/ServiceDetailPage.tsx` (904), `(main)/find-freelancer/page.tsx` (903), `dashboard/FreelancerOrderDetailModal.tsx` (877), `layout/main/navbar/index.tsx` (842), `dashboard/OrderDetailModal.tsx` (831), `dashboard/client/DsahboardContent.tsx` (760).
- **By directory (files):** `ui/inputs` 21 · `payment` 12 · `dashboard/freelancer/ServiceForm` 11 · `(main)/dashboard/freelancer` 8 · `customOrders` 7 · `(main)/explore-services` 7 · `layout/main/navbar` 6 · `jobs` 6 · `dashboard` 6 · `(main)/dashboard/client` 6 · `layout/card` 4 · `messages`/`kyc`/`auth` 3 each · the rest 1–2 each (auth pages, settings, proposals, jobs, find-freelancer, checkout, kyc, messages, orders, custom-orders, notifications, email-verified, `types/`, `src/app/layout.tsx`).
- **Already Panda (no MUI):** the 13 homepage sections, `kick-air-university`, `why-kick-air`, `layout/main/Footer.tsx`, and a small kit in `components/ds/` (`Badge`, `Button`, `Card`, `Dialog/Modal`, `Link`, `Menu`, `Tabs`, `Text/Heading`, `Tooltip`) that only 2 files use today. The admin console has its own primitives in `components/admin/ui.tsx` on `--td-*` CSS variables.
- **Token systems in play:** `src/theme.ts` (`tokens`, 45 importers, also the MUI `theme` for the root `ThemeProvider`), `components/ui/inputs/tokens.ts`, `panda.config.ts` (slate palette), and the admin `--td-*` set. Pick one for the site (Panda tokens + the `ds` kit is the obvious base; the admin `--td-*` look is what Kimleng likes visually).
- **Packages to remove at the end:** `@mui/material`, `@mui/icons-material`, `@mui/material-nextjs` (only `src/app/layout.tsx` uses it, for `AppRouterCacheProvider`), `@emotion/react`, `@emotion/styled`, `@emotion/cache`. Icons: the console uses `lucide-react`; migrate icons to it as pages move.

### Suggested order
1. **`components/ui/inputs/`** first (21 files, 26 importers) — the form kit every page uses. Re-implement the same props on Panda so callers change little.
2. **Shared layout & kits:** navbar (6), `layout/card` (4), `layout/dashboard` (2), `dashboard/ManagementCard.tsx`, `notifications/shared.tsx` (icons + tones), `GlobalNotificationToast` (also mounted in the admin shell).
3. **Flows by area:** auth (4 pages + `components/auth`) → dashboards (client, freelancer, orders, order modals, `OrderRecord` — also embedded on the admin dispute page) → services (ServiceForm 11, explore, detail, checkout, `payment` 12) → jobs/proposals → custom orders → messages → kyc → settings → find-freelancer → notifications/email-verified.
4. **Root layout:** drop `AppRouterCacheProvider` + `ThemeProvider`, delete `src/theme.ts`, flip `preflight: true` in `panda.config.ts` (it's off on purpose while MUI is present — see the comment there), uninstall the packages, run `tsc` + `eslint` + a real `next build`.

### Progress
- **2026-09-07 (evening) — slice 1 of the inputs kit is Panda.** `FieldShell`/`FieldLabel`/`FieldHelper`, `TextInput`, `TextArea` (own autosize hook, MUI `TextareaAutosize` parity), `PasswordInput`, `SearchInput`, `CurrencyInput`, `PhoneInput`, `OtpInput` no longer import `@mui/*`. The shared look lives in `components/ui/inputs/field.ts` (`fieldRoot` cva with `size`/`multiline`, `fieldControl`, `fieldTextarea`, `fieldAdornment`, `fieldIconButton`, `fieldLabel`, `fieldHelper`), all on the Panda tokens (`field`, `border`, `accent`, `radii.input`, `shadows.focusRing`…). Error/disabled states are driven by `data-invalid` / `data-disabled` on the root and `aria-invalid` on the control. Public props are unchanged, so no caller changed; `onBlur`/`onKeyDown` now type against `HTMLInputElement`. Icons in the kit are `lucide-react` (`Eye`/`EyeOff`, `Search`/`X`). Checked visually against the MUI build on sign-in / sign-up / explore-services (pixel-level match apart from the outlined eye icon) plus a throwaway preview of every state.
- **2026-09-07 (late) — slice 2 done: the Ark-backed inputs.** `SelectInput`, `MultiSelectInput` (Ark `Select`, `createListCollection`; string values internally, caller's `string | number` kept in `raw`), `Switch`, `Checkbox` (Ark), `DatePicker` (same hand-rolled calendar, popup on Ark `Popover`), `FileUpload` (plain Panda, ref-in-render lint fixed) and `SegmentedControl` (Panda) are MUI-free. Shared popup recipes live in `inputs/field.ts` (`fieldTrigger`, `fieldTriggerText`, `fieldIndicator`, `fieldPositioner`, `fieldPopup`, `fieldOptionList`, `fieldOption`, `fieldOptionCheck`, `fieldOptionBox`, `fieldChip`). **Popups render inline, not in a `<Portal>`** — MUI `Dialog`'s focus trap yanks focus back from anything portaled outside it and closes an Ark popup on open (ProfileContent's `PfDialog`/language dialog host the kit); `positioning.strategy = "fixed"` still escapes overflow clipping and `--z-index`/`zIndex` 1400 sits above MUI's 1300. Switch to `<Portal>` when the last MUI Dialog is gone. Deleted (no importers): `NumberInput`, `RadioGroupInput`, `Slider`. Verified with a CDP-driven headless-Chrome script (21 checks: open/pick/close, multi toggle, Escape, calendar panes, switch/checkbox, and both popups inside a MUI Dialog) plus screenshots.
- **2026-09-08 — slice 3 done: the inputs kit is MUI-free.** `AutocompleteInput` and `MultiAutocompleteInput` run on Ark `Combobox` (inline popup like the selects; filtering is a local `query` state that resets on close so a strict field snaps back to its value on blur/Escape; `freeSolo` reports typed text on every keystroke; the multi keeps the old Enter semantics — exact case-insensitive label match selects, anything else calls `onCreate`, via its own `onKeyDown` because Zag does not commit custom values on Enter), `TagInput` on Ark `TagsInput` (Enter/comma/paste add, Backspace×2 removes, `suggestions` become a native `<datalist>`). `inputs/tokens.ts` now only holds the colour object + `FieldBaseProps` (`fieldSx`/`FOCUS_RING*` deleted); the sign-up page's inline MUI `Select` became a `SelectInput` (new optional `className` prop on `SelectInput`). Verified with a 40-check CDP script (typing, filtering, pick, clear, revert, create, chip removal, tags, and a combobox inside a MUI Dialog) plus screenshots.
- **2026-09-08/09 — full removal in progress (Kimleng: "do all of those tasks, step by step").** Groundwork: `MIGRATION-PANDA.md` (repo root) is the rulebook every migration follows; the ds kit gained `IconButton`, `Spinner`, `Avatar`, `Alert`, `Divider`, `Skeleton`, `Progress`, `Pager`, `Drawer`, `Popover`, `Toast` (`toast.*` + `<AppToaster/>` mounted in `src/app/layout.tsx`), `EmptyState/Loading/ErrorState`, `Rating`, `Accordion`, `Indicator`; `panda.config.ts` has keyframes (`spin fadeIn slideInRight slideInLeft slideUp pop pulse`). Baseline screenshots of all 34 routes were taken with `shot.cjs` (session scratchpad; it injects a Sanctum test token for Bob #4 / John #2 — tokens named `mui-migration-shot`, **delete them via tinker when the migration is done**).
  - **Lesson:** fanning out 9 parallel agents (then 4) hit the session rate limit twice and burned the budget; the agents were killed before their own verification/report step. **Resume with at most 2 agents at a time, or do areas directly.**
  - **Landed & MUI-free (tsc + eslint clean, 2026-09-09): step 2 is complete.** Navbar (`layout/main/navbar/*` incl. MobileDrawer, bells, WalletChip), `GlobalNotificationToast` (now `toast.info` via ds Toast), `notifications/shared.tsx` + `PushToggle` + `app/notifications`, `layout/card/*` (ServiceCard, StatsCard, FreelancerCard, FreelancerListCard + `freelancerCardKit.ts`), `layout/dashboard/*` (DashboardHeader, DashboardTabs on Ark Tabs), `dashboard/ManagementCard.tsx` (Kebab on Ark Menu; exports `mgCard` Panda class and keeps `mgCardSx` as a plain object for its two MUI callers until they move), `category/CategoryPicker`, `ui/ReviewCard` + `ui/freelancerProfilePage`, `profile/profileKit` + `LevelDialog`, auth (`app/auth/*`, `components/auth/*` + new `authKit.tsx`, `email-verified`). **Kimleng is checking these pages manually** (list in the 2026-09-09 chat: home, sign-in/up, forgot, reset, email-verified, notifications, client & freelancer dashboards incl. Profile tab, find-freelancer grid+list, freelancer profile, explore-services, freelancer Services tab).
  - **2026-09-09 (late) — payment is MUI-free (13 files, 89 → 76).** `components/payment/*` (PriceRow, Annot, PaymentFooterLogos, PayLogo, StatusChip, AbaMethodSelector, PaymentOption, usePaymentProcessing, PaymentResult, MockAbaPayWayPopup, TopUpDialog, WithdrawDialog) + `purchase/PurchaseGate`. Two Opus agents in parallel; props/exports unchanged so checkout, FinanceView, Footer and ServiceDetailPage did not change. MUI `Dialog` → ds `BareModal` (imported by path; it is not re-exported from `ds/index.ts`), `CircularProgress` → ds `Spinner` (grey ring + arc instead of MUI's bare arc), icons → lucide. `payment/pill.ts` (`pillButton` cva, exact MUI Button base metrics) is now the CTA recipe for the whole area. Because Emotion ran in `@layer mui`, the unlayered `globals.css` `p/h1–h6 { margin:0 }` already beat every `sx` margin on those elements, so those dead margins were dropped rather than ported. Two pre-existing React Compiler lint errors in the folder were fixed on the way: TopUpDialog's open-reset effect (now the render-time "adjust state on prop change" pattern) and QrGlyph's inner `Finder` component (hoisted to module level). `tsc` + `eslint` clean on `components/payment` + `components/purchase`. **Not yet eyeballed in a browser** (dev server was down): top-up presets/custom toggle + disabled CTA, withdraw over-balance warning / spinner / success card, PaymentResult waiting/success/failure, mock PayWay popup (navy header, QR pane, card form), purchase gate logged-out (Google pill + "or" divider), become-client busy state, KYC step pills.
  - **2026-09-09 (night) — messages is MUI-free (7 files, 76 → 69).** `components/messages/{MessageBubble,ConversationList,ChatView}` + `app/(main)/dashboard/{client,freelancer}/messages/page.tsx` + `{client,freelancer}/MessagesContent.tsx`. Two Opus agents in parallel; both hit a 600 s stream-watchdog stall right after writing their last file, but the files were complete, `tsc` is clean project-wide and `eslint` is clean on the slice (one pre-existing warning: ChatView's `scrollToBottom` is defined but its call was already commented out at HEAD; left as-is to keep behaviour). ChatView's composer replaces MUI `TextareaAutosize` with a plain textarea + `useLayoutEffect` autosize capped at 4 rows. Props/exports unchanged. **Not yet eyeballed in a browser:** conversation list + search, own vs other bubbles, avatars, unread badge, attachment/offer chips, composer autosize + Enter-to-send, empty/loading states, the mobile back button and the two-pane split on the messages pages, and the "see all" arrow link on the dashboard Messages tab.
  - **2026-09-09 (night) — review fixes, then everything committed.** Kimleng eyeballed every migrated page and found four issues, all fixed: (1) the migrated navbar container was content-box (`w: 100%` + `px`) and overflowed the viewport by its padding on every page (hamburger pushed off-screen, sideways scroll); fixed with `boxSizing: "border-box"` and the same fix applied by sweep to DashboardTabs container, layout/card ServiceCard + FreelancerCard, auth verifyKit, PaymentResult, PaymentOption, ds Button `full`, homepage FAQ trigger + hero search input, and admin `ui.tsx` `page`/`fieldBase` + LoginPage card. **Rule for every future migration: any Panda element with `w: 100%` + horizontal padding needs `boxSizing: "border-box"` until preflight is on.** Verified with a CDP overflow finder at 390/800px on `/`, `/explore-services`, `/admin/login` (scrollWidth == viewport, zero offenders). (2) ServiceDetailPage's two "Contact Freelancer" buttons never had a handler (pre-existing): now `handleContact` → sign-in for guests, else `api.startConversation(freelancer_profile.user_id)` → `/dashboard/client/messages?id=…`, disabled on own service. (3) Freelancer ProfileContent's profile-strength card now hides at 100% (pre-existing). (4) JobPostForm now scrolls to the first invalid field on publish (`ERROR_ANCHORS` + `scrollToFirstError`, same as ServiceForm; pre-existing gap). All of the above plus the whole migration to date was committed and pushed to `main`.
  - **2026-09-12 — wave 1 on branch `mui-removal`: service form, KYC and custom orders are MUI-free (27 files, 69 → 42).** Kimleng upgraded to Max x20 and asked for the rest wave by wave without reviewing between waves; the work goes on the `mui-removal` branch (off `main`, so a half-migrated site never auto-deploys) and is committed per wave. Three Opus agents in parallel, one per area, each with its own CDP interaction suite run on the MUI build first and then on the port (42 / 42 / 24+9 checks), before/after screenshots at 1280 and 390, `scrollWidth === innerWidth` everywhere. Service form: `dashboard/freelancer/ServiceForm/*` (11), `DraftCard`, `freelancer/ServiceCard` (now `cx(mgCard, …)` + `[data-muted]`), `ServicesContent`; dialogs on `BareModal`, the terms checkbox is the kit `Checkbox` (the only visible geometry change). KYC: `kyc/{KycWizard,KycStatusViews,CameraCapture}`, `dashboard/kyc/page`, `KycBanner` (ds `Alert`); the unused colour object in `kyc/tokens.ts` was deleted (only `useObjectUrl` remains). Custom orders: `customOrders/*` (7) + `dashboard/custom-orders/[id]/page`; `coCard`/`coLabel` are Panda class strings now, new kit exports `CoInput`/`CoTextArea`/`CoSelect` (Ark) and `coBtn`/`coIconBtn`; `DirectOfferDialog`/`RequestCustomOrderDialog` props unchanged so their still-MUI hosts did not move. Two new rulebook gotchas came out of this wave (static extraction of imported constants; attribute-selector overrides) — see MIGRATION-PANDA.md. **Pre-existing bugs logged, not fixed:** `FundMilestoneDialog` parses the formatted `available_balance` string (`Number("18,350.00")` → NaN, so the balance shows $0.00 and the "short by / top up" branch is unreachable; use `available_balance_raw`); `useObjectUrl` revokes its blob URL on StrictMode's first unmount so KYC review thumbnails are broken in dev; service cover thumbnails 404 on the services list; the Workspace escrow summary keeps its 4-column grid at 390px (the `mobile` variant exists but no caller passes it); `ServiceCard`'s `role="button"` div has no key handler; the "Terms of Service" text in the service form has no link. Remaining: explore + `ServiceDetailPage` (9 files), jobs & proposals (12, incl. client `PostServiceContent` which is the post-a-job tab and the last `mgCardSx` user), find-freelancer (2), settings (1), dashboard tabs (9), orders (7, client/freelancer twins share 65–75 % of their lines), then the root teardown.
  - **2026-09-12 — wave 2 on `mui-removal`: explore + service detail, jobs & proposals, find-freelancer and settings are MUI-free (24 files, 42 → 18).** Three Opus agents in parallel again. Explore: `explore-services/*` (8) + `service/ServiceDetailPage`; new ds primitives `Slider` (Ark, MUI geometry) and `RadioGroup` (+ `RadioGroupPrimitive`, `radioControl`, `radioItem`), both wired into `ds/index.ts`; the explore page's pre-existing hydration error is gone (it was MUI `useMediaQuery(..., { noSsr: true })` in FiltersSidebar, now `useSyncExternalStore(matchMedia)`); breakpoints follow Panda `lg` = 1024 instead of MUI 1200. Jobs: `components/jobs/*` (6), `/jobs`, `/jobs/[id]`, `/proposals/[id]`, `/dashboard/jobs/[id]/proposals`, `ProposalsContent`, client `PostServiceContent` (`cx(mgCard, …)`; **`mgCardSx` is deleted from ManagementCard**); new ds `BottomSheet` (Ark dialog anchored bottom, for the job board's mobile filter sheet), wired into the index. **One behaviour fix landed with it:** `/proposals/[id]` used to `router.replace("/login")` (a 404 route) during render whenever `user` was null — including while auth was still loading — so a fresh tab could never open it; it now waits for `loading` and redirects guests to `/auth/sign-in` from an effect. Find-freelancer + settings: 3 files, no new primitives; the find-freelancer error state's "Back to Freelancers" pill was black-on-blue (unlayered `a { color: inherit }`) and now uses `!important` white. Seeded `[mui-test]` jobs/proposals for the jobs slice were purged afterwards (DB back to job 1 only, 0 proposals). **Pre-existing bugs logged, not fixed:** `GET /api/job-posts/{id}` is public so `my_proposal` is never attached and the "already applied" / edit / withdraw states on `/jobs/[id]` are unreachable; `JobPostController::destroy` only sets `status = cancelled`, so "Delete draft" leaves a Cancelled card behind; `formatCurrency(null)` prints "null — null" for budget-less jobs; the mobile job filter sheet shows "Filters" twice; `/api/services` returns no `meta` so the explore pager never renders; `/find-freelancer/[id]` shows the raw Laravel "No query results" message; the settings Deactivate button has no handler; MUI `Stack spacing` gaps on `<p>` elements never rendered (Expertise / "N selected" overlap) and were ported as rendered. Remaining: dashboard tabs (9 files) and orders (7 files), then the root teardown.
  - **2026-09-13 — wave 3 on `mui-removal`: dashboard tabs and orders are MUI-free (16 files, 18 → 2).** Only `src/app/layout.tsx` and `src/theme.ts` still import `@mui/*`. Two Opus agents (the first pair was killed by the harness a minute after launch without a report — check `git status` and the transcript mtime before assuming an agent is working). Dashboard tabs: both dashboard `page.tsx`, client `DsahboardContent`/`ProfileContent`, freelancer `DashboardContent`/`ProfileContent`/`LevelContent`, `NotificationsContent`, `FinanceView`; every dialog (language, education, certification, portfolio, `PfDialog`) is on `BareModal` and the inputs kit's inline popups were verified opening inside them, so **the site has no MUI `Dialog` left** (the kit could move to `<Portal>` now; left inline deliberately, it works and is verified). Orders: `OrderRecord` (props unchanged; still embedded by `admin/DisputeDetailPage`, pixel-diffed there), both `OrdersContent`, both order pages, both detail modals; slice-specific style kits live in `components/dashboard/{orderListKit,orderModalKit,orderPageKit}.tsx` (the agent had put them and a second modal primitive that copied MUI's dialog chrome under `ds/`; the lead moved the kits and replaced that primitive with `BareModal` so order dialogs match the custom-order ones — the 76-check CDP suite passes identically afterwards). Breakpoints follow Panda (`md` 768 / `sm` 640) where MUI used 900 / 600. **Dead code found (already dead on `main`, ported anyway, delete when convenient):** `components/dashboard/OrderDetailModal.tsx` + `FreelancerOrderDetailModal.tsx` (~1,540 lines, no importer — the unified lists navigate to the order pages), `components/dashboard/NotificationsContent.tsx` (no importer), `components/dashboard/orderModalKit.tsx` (only used by the two dead modals). **Pre-existing bugs logged, not fixed:** `layout/dashboard/DashboardHeader`'s Panda `Container` lacks `boxSizing: "border-box"`, so with preflight off its title sits 24px left of the tabs on every dashboard page (moot once preflight is on); both order pages fetch the whole orders list and `.find()` the id (no single-order endpoint), so a non-participant just sees "Order not found"; both lists print "Delivery: 3 days days"; `FinanceView`'s payment-methods divider never rendered (MUI `height: 1` = 100 %); the freelancer Finance tab overflowed at 390 on MUI (fixed by the port via `minW: 0`); `formatSessionTime`/`ReviewOffer` read the clock during render; dead "View All" buttons on both dashboards; the client Profile tab shows "Unsaved" on load; `OrderRecord`'s note pill and every `SEC_LABEL` margin are dead (globals.css) and were ported as rendered. Next: the root teardown (layout providers, `theme.ts`, `preflight: true`, layered `globals.css`, uninstall `@mui/*` + `@emotion/*`, real `next build`).
  - **2026-09-13 — root teardown: MUI and Emotion are gone (141 → 0 files).** `src/app/layout.tsx` no longer wraps the app in `AppRouterCacheProvider`/`ThemeProvider`; `src/theme.ts` is deleted; the `mui` cascade layer is dropped from `globals.css`; `@mui/material`, `@mui/icons-material`, `@mui/material-nextjs`, `@emotion/react`, `@emotion/styled`, `@emotion/cache` are uninstalled (package.json + lock). `tsc` clean; `eslint .` has 0 new problems (5 pre-existing errors: 4 in the generated `styled-system/` folder, which the lint config should ignore, and `src/hooks/useServiceListingLive.ts:16` reading a ref during render). **Panda `preflight` stays off — measured, not assumed.** Flipping it (plus removing the bare `globals.css` rules) was captured against a 70-shot baseline (35 routes × 1280/390): every page changed. `html { line-height: 1.5 }` re-flowed all text (fixable with a `base`-layer override), but the other two effects are structural: `box-sizing: border-box` on everything shrinks every `maxW` + padding container (homepage service cards 379 → 352 px; the admin console's phone layout collapsed to one letter per line), and the `p`/`h*`/`ul` margins that the bare rules used to cancel come alive (homepage +10 px per section, university +242 px, why-kick-air +363 px at 1280). That is a restyle pass over the pre-rulebook Panda files (13 homepage sections, `kick-air-university`, `why-kick-air`, `layout/main/Footer`, `components/admin/ui.tsx` + pages, `layout/dashboard/DashboardHeader`), so it is left as a follow-up; the config, the bare rules and the rulebook document the decision. After the revert all probes are pixel-identical to the baseline. Temporary Sanctum tokens named `mui-migration-shot` (users 1, 2, 4) were created for screenshots and deleted at the end. **Merged to `main` on 2026-09-13** together with the `website-v2` spike (merge commit 464d651; `mui-removal`, `website-v2-redesign` and the stale `progress` branch were deleted at Kimleng's request, so `main` is the only branch). Note the spike is therefore reachable on production at `/website-v2`; `rm -rf src/app/website-v2` and drop `motion` from package.json to retire it. **Follow-ups worth doing next:** delete the dead components (`OrderDetailModal`, `FreelancerOrderDetailModal`, `NotificationsContent`, `orderModalKit`, `explore-services/SortBar`, `ServiceForm/RequirementItem` + `RequirementsSection`), add `styled-system/**` to the eslint ignores, move the inputs kit's popups to `<Portal>` now that no MUI focus trap exists, and the preflight flip above.
  - **Not started (as of 2026-09-09):** finance, jobs/proposals, custom orders, KYC, services (ServiceForm, explore-services, detail, checkout), dashboards (client/freelancer content, order modals, order pages, OrderRecord, custom-orders page), find-freelancer pages, settings, misc single pages — 69 files. Then step 4 (root layout providers, `theme.ts`, `preflight: true`, layer the `globals.css` resets, uninstall `@mui/*` + `@emotion/*`, `next build`).
- **Panda + preflight-off gotchas learned:** inputs/textareas are `content-box` (set `boxSizing`); an Ark part hidden via the `hidden` attribute needs `"&[hidden]": { display: "none" }` if the recipe sets `display`; nested selector keys must start or end with `&` (`"[data-state=checked] > &"` ok, `"[x] > & svg"` is not — nest `"& svg"` inside instead); `globals.css` `* { scroll-behavior: smooth }` animates `scrollIntoView` (use `behavior: "instant"` in test drivers).

### Gotchas
- `globals.css` has **unlayered** `p, ul, h1–h6 { margin: 0 }` and `a { color: inherit }` rules that beat Panda utility classes (Panda layers are `reset, base, tokens, recipes, utilities`). A `<p className={css({ mt: … })}>` silently gets no margin — use a `<div>`, padding, or the ds `Link`. Kept on purpose after the MUI removal: see the preflight note in `panda.config.ts`.
- Panda scans `./src/**` via the PostCSS plugin; no codegen step is needed for new `css()` calls, but new **tokens/recipes** in `panda.config.ts` need `npm run panda`.
- The React Compiler lint rules are on: no `Date.now()`/`new Date()` in render (use a client-only `useSyncExternalStore` like `OverviewPage`), no `setState` inside `useEffect` bodies.
- Keep `OrderRecord` as the single order history while porting it — same rule as before.

## Design workflow note
Some pages were built from **Claude Design** handoff bundles. **Find Freelancers** and **Order Detail** came from those bundles.

---
At handoff: both repos are committed and pushed to `main` (Telegram Gateway hardening, the admin console replacement and its API changes); this handoff note's MUI-removal section was written afterwards and may still be uncommitted. Confirm git state before starting.
